---
"@supabase/pg-topo": patch
---

Recognize additional built-in PostgreSQL opclass catalog support objects, including date/time `in_range` overloads, cross-type btree support operators, and opclass datatype validation for support routines.

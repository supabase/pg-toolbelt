{
  "name": "pg-toolbelt",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "overrides": {
    "cpu-features": "file:./.stubs/cpu-features"
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "check-boundaries": "bun scripts/check-effect-boundaries.ts",
    "e2e": "./scripts/e2e-docker.sh",
    "coverage": "bun scripts/coverage.ts",
    "check-types": "bun run --filter '*' check-types && bun run check-boundaries",
    "format-and-lint": "biome check . --error-on-warnings",
    "knip": "bun run --filter '*' knip",
    "test:pg-delta": "bun run --filter '@supabase/pg-delta' test",
    "test:pg-topo": "bun run --filter '@supabase/pg-topo' test",
    "version": "changeset version"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "@changesets/cli": "^2.29.8",
    "@types/bun": "^1.3.9",
    "bun": "^1.3.9",
    "nyc": "^18.0.0"
  }
}
